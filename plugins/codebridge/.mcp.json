{
	"mcpServers": {
		"codebridge-harness": {
			"type": "stdio",
			"command": "powershell.exe",
			"args": [
				"-NoProfile",
				"-ExecutionPolicy",
				"Bypass",
				"-File",
				"${workspaceFolder}\\scripts\\windows\\serve-harness-mcp.ps1"
			],
			"cwd": "${workspaceFolder}"
		}
	}
}